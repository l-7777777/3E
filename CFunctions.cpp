#include "CFunctions.hpp"
namespace CStr {
    char *split(char *str, char delimiter, int max) {
        char *toReturn[max];
        for (int i = 0; i < sizeof(str); i++) {
            if (str[i] == delimiter) {
                
            }
        }
        return *toReturn;
    }
}